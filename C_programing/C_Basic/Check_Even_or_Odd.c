#include <stdio.h>
#include <stdlib.h>

int main()
{
    int num;

    printf("ennter an integer you want to check\n");
    scanf("%d",&num);
    if(num%2==0)
    printf("the unmber is even");
    else
    printf("the unmber is odd");


    return 0;
}
